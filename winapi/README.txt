На рабочем столе 4 папки: 3 с лабораторными и 1 с РГЗ.

В лабораторной работой #4(pipe_project) необходимо
запустить first_process.exe, введя в консоль:


В лабораторной работе #5(mailslot_project) необходимо
в первую очередь запустить server.exe, введя в консоль:

Потом запустить client.exe:

В консоле с вызванной клиентской программой следует написать:


В лабораторной работе #8(sockets) необходимо
в первую очередь запустить server.exe:

Потом client.exe:

В консоле с вызванной клиентской программой следует написать:
file.txt de или one_more_file.txt ru

В РГЗ(service) сначала нужно создать и запустить службу.
Сделать это нужно следующими командами:
sc create SampleService binpath=
sc start SampleService
Далее необходимо запустить client.exe:

После ввести:
file.txt de
После завершения работы со службой в консоль следует ввести:
sc stop SampleService
sc delete ServiceSample